# admin
admin/.env
admin/node_modules/
admin/dist/
.cursor
.idea
.DS_Store

# Keep GitHub Actions publishable
!.github/
!.github/workflows/
!.github/workflows/**

# Docker bind-mount for rpcnode-server (toolkit.db, panel.htpasswd, …)
/data/

# Local release staging (scripts/release.sh → dist/release/*.jar)
/dist/

# app (also listed in app/.gitignore)
app/build/
app/.gradle/
app/.idea/
app/.kotlin/
app/database/
*.log
