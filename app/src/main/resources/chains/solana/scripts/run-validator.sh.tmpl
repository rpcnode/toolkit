#!/usr/bin/env bash
set -euo pipefail
# Shipped with the Solana client (panel clients/<env>/ + host node_dir).
# Start fills {{placeholders}} into run-validator.sh — do not hand-edit the rendered file.
export PATH={{bin_dir}}:${PATH}
BIN={{bin}}
if [[ ! -x "$BIN" ]]; then echo "solana binary missing: $BIN" >&2; exit 127; fi
exec "$BIN" \
{{exec_flags}}
